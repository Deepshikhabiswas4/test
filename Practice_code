# BGL PIPELINE

import re
import sys
import os
from datetime import datetime, timedelta
from pathlib import Path
from py4j.java_gateway import java_import
from pyspark.sql.functions import col,split,substring,concat,regexp_replace,when,length,lit,count,substring,sum,input_file_name,substring_index
from delta.tables import DeltaTable
from common.createSpark import create_spark_session
from common.fileUtil import get_hdfs_base
from common.fileUtil import paths_for_read
from common.dateUtil import get_etl_date
from common.processRun import get_process_run_id
from common.processRun import get_run_id
from common.constants import Process
from common.logger import setup_logger
# from common.log_etl_modified import log_etl
from common.create_path_if_not_exists import create_path_if_not_exists
from common.read_write_oracle import read_oracle, write_oracle

from common.pattern import read_patterns
# ==========================================================
# GET LAST PROCESSED DATE
# ==========================================================
def get_last_processed_date(spark):

    query = """
    (
        SELECT MAX(FILE_DATE) AS LAST_PROCESSED_DATE
        FROM BGL_FILE_TRACKER
        WHERE STATUS='PROCESSED'
    )
    """
    # print("===========================Before oracle read==========================")
    tracker_df = read_oracle(spark, query)
    # print("===========================After oracle read==========================")
    row = tracker_df.collect()[0]
    return row["LAST_PROCESSED_DATE"]


# ==========================================================
# CHECK IF FILE ALREADY PROCESSED
# ==========================================================

def is_processed(spark, file_name):

    query = f"""
    (
        SELECT COUNT(*) CNT
        FROM BGL_FILE_TRACKER
        WHERE FILE_NAME='{file_name}'
        AND STATUS='PROCESSED'
    )
    """
    tracker_df = read_oracle(spark, query)
    return tracker_df.collect()[0]["CNT"] > 0


# ==========================================================
# MAIN_PIPELINE
# ==========================================================
try:
    PROCESS_ID = Process.CBS_ALL_DATA
    HDFS_BASE = get_hdfs_base()
    print(f"HDFS_BASE: {HDFS_BASE}")
    spark = create_spark_session("BGL_PIPELINE", HDFS_BASE)

    today = get_etl_date(spark)
    # today=datetime(2026,1,30).date()
    posting_date_str = today.strftime("%d-%m-%Y")
    today_str = posting_date_str

    process_run_id = (
        sys.argv[1] if len(sys.argv) > 1
        else get_process_run_id(spark, PROCESS_ID, posting_date_str)
    )
    run_id = 1 
    

    print("====================================================")
    print("BGL Pipeline Started")
    print("====================================================")
    #log_etl(spark,process_run_id,"bgl_pipeline",1,"BGL pipeline started") 

    print(f"Process ID            : {PROCESS_ID}")
    print(f"HDFS Base Path        : {HDFS_BASE}")

    # ==========================================================
    # GET LAST PROCESSED DATE
    # ==========================================================
    print("Fetching Last Processed Date from BGL_FILE_TRACKER.")
    last_processed_date = get_last_processed_date(spark)
    print(f"ETL Date              : {today}") #31st march
    print(f"Last Processed Date   : {last_processed_date}") #none

    if last_processed_date is None:
        print(
            "No previously processed records found. "
            "Pipeline will start from the ETL Date."
        )
        from_date = today #31st march
    else:
        from_date = last_processed_date + timedelta(days=1)
    
    to_date = today #31st march
    
    print(f"From Date             : {from_date}") #31st march
    print(f"To Date               : {to_date}") #31st march

    # ==========================================================
    # VALIDATE ETL DATE
    # ==========================================================
    if last_processed_date is not None and today <= last_processed_date:
        print(
            f"Invalid ETL Date. ETL Date ({today}) must be greater than "
            f"the Last Processed Date ({last_processed_date})."
        )

        print(
            "ETL Date should always be ahead of the Last Processed Date. "
            "Pipeline execution cannot continue."
        )

        raise Exception(
            "ETL Date should always be ahead of the Last Processed Date."
        )
    else:
        print("Date validation completed successfully.")
        print(f"Pipeline will process files from {from_date} to {to_date}.")

    # ==========================================================
    # HDFS
    # ==========================================================  
    java_import(spark._jvm, "org.apache.hadoop.fs.FileSystem")
    java_import(spark._jvm, "org.apache.hadoop.fs.Path")
    fs = spark._jvm.FileSystem.get(spark._jsc.hadoopConfiguration())

    current_date = from_date #31st march
    
    while current_date <= to_date: #31st march <= 31st march
        print("Entered while loop 1111111111111111111111111111111111111111111111111")
        folder_date = current_date.strftime("%d-%m-%Y") #31st march
        # input_folder = f"{HDFS_BASE}/CBS-FILES/31-01-2026/misc"
        input_folder = f"{HDFS_BASE}/CBS-FILES/{folder_date}/misc/BGL*"
        allpaths=[]
        allpaths.append(input_folder)

        # input_folder = f"{HDFS_BASE}/CBS-FILES/05-04-2026/misc/BGL*/BGL*"
        found,unfound = read_patterns(spark,allpaths)

        print(found)
        print(unfound)


        # if len(unfound)>0:
        #     print(f"{folder_date} folder not present.") 
        #     tracker_df = spark.createDataFrame([("NOT FOUND", current_date, "FILE NOT PRESENT")],["FILE_NAME", "FILE_DATE", "STATUS"])
        #     # tracker_df.show(20,False)
        #     write_oracle(tracker_df, "BGL_FILE_TRACKER") 

        # ======================================================
        # DATALAKE PATH
        # ======================================================
        year = current_date.strftime("%Y")
        month = current_date.strftime("%b").upper()
        day = current_date.strftime("%d")
    
        output_path = (f"{HDFS_BASE}/druid-data-lake/{year}/{month}/{day}/BANCS24/BGL")
        summary_output_path = (f"{HDFS_BASE}/druid-data-lake/{year}/{month}/{day}/PRODUCT_WISE/datalake")
        create_path_if_not_exists(spark, output_path)
        create_path_if_not_exists(spark, summary_output_path)
    
        # ======================================================
        # LOOP THROUGH FILES
        # ======================================================
        
        try:
                # ==================================================
                # READ FILE
                # ==================================================
                file_df = spark.read.format("text").load(input_folder).withColumn("FILE_NAME", regexp_replace(substring_index(input_file_name(), "/",-1), r"\.txt.gz$",""))

                file_name = file_df.select("FILE_NAME").first()["FILE_NAME"]
                
            
                file_df.show(20, False)

                
                parts = split(col("value"), r"\|")
    
                # ==================================================
                # PARSE FILE
                # ==================================================
    
                parsed_df = file_df.select(
                    concat(substring(parts.getItem(0), 8, 7),parts.getItem(1)).alias("ACCOUNT_NO"),
                    parts.getItem(1).alias("BRANCH_CODE"),
                    parts.getItem(2).alias("PRODUCT"),
                    parts.getItem(3).alias("SEGMENT"),
                    parts.getItem(4).alias("AMOUNT"),
                    parts.getItem(5).alias("GLCC"),
                    parts.getItem(6).alias("ACCOUNT_NAME"),
                    when(regexp_replace(parts.getItem(7), "^0+", "") == "","0").otherwise(regexp_replace(parts.getItem(7), "^0+", "")).alias("STATUS")
                )
                # parsed_df.cache()
                total_row_count = parsed_df.count()
                print(f"Rows parsed from {file_name} :{total_row_count} ")
                
                parsed_df.show(200, False)
    
                # ==================================================
                # WRITE TO DELTA
                # ==================================================
    
                # parsed_df.write \
                #     .format("delta") \
                #     .mode("overwrite") \
                #     .save(output_path)
    
                print(f"{file_name} written successfully.")
                

                try:

                    type_name = file_name.split("_")[0].upper()
                    file_date = datetime.strptime(
                        file_name.split("_")[2],
                        "%d%m%Y").strftime("%d-%m-%Y")
                    
                    summary_df = (parsed_df.withColumn("CGL",substring(col("GLCC"),9,10)).withColumn("CURRENCY",substring(col("GLCC"),6,3)).withColumn("TYPE",lit(type_name)).withColumn("DATE",lit(file_date)).withColumn("AMOUNT",col("AMOUNT").cast("decimal(20,2)")).groupBy("CGL","CURRENCY","PRODUCT","TYPE","DATE").agg(sum("AMOUNT").alias("BALANCE")).select("CGL","CURRENCY","PRODUCT","BALANCE","TYPE","DATE"))
                    
                    # summary_count = summary_df.count()
                    summary_df.show(200, False)
                    print("Summary dataframe created.")
                    # print(f"Summary records created : {summary_count}")


                    if(run_id>1):
                        print(f"Rerun detected (Run id: {run_id}). "
                                    f"Deleting existing {type_name} for {file_date}")
                        delta_table = DeltaTable.forPath(spark, summary_output_path)
                        delta_table.delete(f"TYPE = '{type_name}' AND DATE = '{file_date}'")
                        print("Existing summary records deleted successfully.")


                    # summary_df.write \
                    #     .format("delta") \
                    #     .mode("append") \
                    #     .save(summary_output_path)
                    
                    print(f"Summary written successfully."
                                f"{type_name} - {file_date}")
        
                except Exception as e:

                    print(f"Error {e} processing SUMMARY DF")
                    spark.stop()
                    sys.exit()


                # ==================================================
                # UPDATE TRACKER TABLE
                # ==================================================
                try:
                    tracker_df = spark.createDataFrame([(file_name,current_date,"PROCESSED",total_row_count)],["FILE_NAME","FILE_DATE","STATUS","ROW_COUNT"])
                    tracker_df.show(20,False)
                    write_oracle(tracker_df,"BGL_FILE_TRACKER")
                    print(f"{file_name} tracker updated.")
            
                except Exception as e:

                    print(f"Error {e} processing tracker ")
                    spark.stop()
                    sys.exit()

    
        except Exception as e:

                print(f"Error processing {file_name}")
                print("Exception occured while processing the file.")

                tracker_df = spark.createDataFrame([(file_name,current_date,"FAILED")],["FILE_NAME","FILE_DATE","STATUS"])
                write_oracle(tracker_df,"BGL_FILE_TRACKER")

        # ======================================================
        # MOVE TO NEXT DATE
        # ======================================================
        current_date += timedelta(days=1)

    print("==========================================")
    print("BGL Processing Completed Successfully")
    print("==========================================")
    #log_etl(spark,process_run_id,"bgl_pipeline",2,f"BGL pipeline completed successfully.")

except Exception as e:
    error_message = str(e)
    print(f"BGL Pipeline Failed Error :{error_message}")
    #log_etl(spark, process_run_id, "bgl_pipeline", 3, error_message)
    raise

finally:

    try:
        print("Stopping Spark Session")
        spark.stop()
        print("Stopped Spark Session")
    except:
        pass




