def update_oracle(spark, query):
    try:
        jvm = spark._jvm

        connection = jvm.java.sql.DriverManager.getConnection(
            oracle_url,
            oracle_user,
            oracle_password
        )

        statement = connection.createStatement()
        statement.executeUpdate(query)

        statement.close()
        connection.close()

        print("Oracle update successful.")

    except Exception as e:
        print("Oracle update error:", str(e))
        raise














if len(unfound) > 0:

    # ==========================================================
    # CHECK IF NOT FOUND RECORD ALREADY EXISTS
    # ==========================================================
    check_query = f"""
    (
        SELECT COUNT(*) AS CNT
        FROM BGL_FILE_TRACKER
        WHERE FILE_DATE = TO_DATE('{output_date}', 'DD-MON-RR')
          AND STATUS = 'FILE NOT PRESENT'
    )
    """

    check_df = read_oracle(spark, check_query)
    not_found_count = check_df.collect()[0]["CNT"]

    if not_found_count == 0:

        print(
            f"No BGL file found for {output_date}. "
            f"Inserting NOT FOUND record."
        )

        tracker_df = spark.createDataFrame(
            [
                (
                    "NOT FOUND",
                    output_date,
                    "FILE NOT PRESENT"
                )
            ],
            [
                "FILE_NAME",
                "FILE_DATE",
                "STATUS"
            ]
        )

        tracker_df.show(20, False)

        write_oracle(
            tracker_df,
            "BGL_FILE_TRACKER"
        )

        print(
            f"NOT FOUND record inserted for {output_date}."
        )

    else:

        print(
            f"NOT FOUND record already exists for {output_date}. "
            f"Skipping duplicate insertion."
        )
